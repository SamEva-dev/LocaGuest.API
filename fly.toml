# Fly.io configuration for LocaGuest API
app = "locaguest-api"
primary_region = "cdg" # Paris

[build]
  dockerfile = "Dockerfile"

[env]
  ASPNETCORE_ENVIRONMENT = "Production"
  ASPNETCORE_URLS = "http://+:8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

  [[http_service.checks]]
    interval = "15s"
    timeout = "2s"
    grace_period = "5s"
    method = "GET"
    path = "/live"

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/ready"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024

# Secrets à configurer via CLI:
# fly secrets set JWT_SECRET="votre-secret-jwt"
# fly secrets set DATABASE_URL="votre-connection-string"
# fly secrets set STRIPE_SECRET_KEY="sk_live_xxx"
# fly secrets set STRIPE_WEBHOOK_SECRET="whsec_xxx"
