name: LocaGuest.API - Build & Promote (staging -> preprod -> prod)

on:
  push:
    branches: ["main"]

concurrency:
  group: locaguest-api-main
  cancel-in-progress: false

jobs:
  build_push:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute image tag
        id: meta
        shell: bash
        run: |
          echo "image_tag=sha-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push (immutable)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/locaguest-api:${{ steps.meta.outputs.image_tag }}

  deploy_staging:
    runs-on: ubuntu-latest
    needs: [build_push]
    environment: staging
    steps:
      - name: Deploy to STAGING (VPS)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail

            IMAGE_TAG="${{ needs.build_push.outputs.image_tag }}"

            COMPOSE_DIR="/opt/locaguest/config"
            COMPOSE_FILE="${COMPOSE_DIR}/docker-compose.yml"
            ENV_FILE="/opt/locaguest/env/.env"

            sudo mkdir -p "$(dirname "$ENV_FILE")"
            sudo touch "$ENV_FILE"
            sudo chmod 600 "$ENV_FILE" || true

            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            upsert_env () {
              local key="$1"
              local value="$2"
              if sudo grep -qE "^${key}=" "$ENV_FILE"; then
                sudo sed -i "s|^${key}=.*|${key}=${value}|" "$ENV_FILE"
              else
                echo "${key}=${value}" | sudo tee -a "$ENV_FILE" >/dev/null
              fi
            }

            upsert_env "LOCAGUEST_API_TAG_STAGING" "${IMAGE_TAG}"

            cd "$COMPOSE_DIR"

            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" up -d caddy
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile staging pull locaguest-api-staging
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile staging up -d locaguest-db-staging locaguest-api-staging
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile staging ps

  deploy_preprod:
    runs-on: ubuntu-latest
    needs: [deploy_staging]
    environment: preprod
    steps:
      - name: Promote to PREPROD (VPS)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail

            IMAGE_TAG="sha-${{ github.sha }}"

            COMPOSE_DIR="/opt/locaguest/config"
            COMPOSE_FILE="${COMPOSE_DIR}/docker-compose.yml"
            ENV_FILE="/opt/locaguest/env/.env"

            sudo mkdir -p "$(dirname "$ENV_FILE")"
            sudo touch "$ENV_FILE"
            sudo chmod 600 "$ENV_FILE" || true

            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            upsert_env () {
              local key="$1"
              local value="$2"
              if sudo grep -qE "^${key}=" "$ENV_FILE"; then
                sudo sed -i "s|^${key}=.*|${key}=${value}|" "$ENV_FILE"
              else
                echo "${key}=${value}" | sudo tee -a "$ENV_FILE" >/dev/null
              fi
            }

            upsert_env "LOCAGUEST_API_TAG_PREPROD" "${IMAGE_TAG}"

            cd "$COMPOSE_DIR"
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" up -d caddy
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile preprod pull locaguest-api-preprod
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile preprod up -d locaguest-db-preprod locaguest-api-preprod
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile preprod ps

  deploy_prod:
    runs-on: ubuntu-latest
    needs: [deploy_preprod]
    environment: prod
    steps:
      - name: Promote to PROD (VPS)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail

            IMAGE_TAG="sha-${{ github.sha }}"

            COMPOSE_DIR="/opt/locaguest/config"
            COMPOSE_FILE="${COMPOSE_DIR}/docker-compose.yml"
            ENV_FILE="/opt/locaguest/env/.env"

            sudo mkdir -p "$(dirname "$ENV_FILE")"
            sudo touch "$ENV_FILE"
            sudo chmod 600 "$ENV_FILE" || true

            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            upsert_env () {
              local key="$1"
              local value="$2"
              if sudo grep -qE "^${key}=" "$ENV_FILE"; then
                sudo sed -i "s|^${key}=.*|${key}=${value}|" "$ENV_FILE"
              else
                echo "${key}=${value}" | sudo tee -a "$ENV_FILE" >/dev/null
              fi
            }

            upsert_env "LOCAGUEST_API_TAG_PROD" "${IMAGE_TAG}"

            cd "$COMPOSE_DIR"
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" up -d caddy
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile prod pull locaguest-api-prod
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile prod up -d locaguest-db-prod locaguest-api-prod
            sudo docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" --profile prod ps
